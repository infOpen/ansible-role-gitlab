---

# Main tasks file for gitlab role

- name: 'INIT | Manage variables to use for our target'
  include: "{{ role_path }}/tasks/manage_variables.yml"
  tags:
    - 'role::gitlab'
    - 'role::gitlab::init'
